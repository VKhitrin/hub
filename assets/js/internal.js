// Internal placeholder config file, example to extend CONFIG.commands below

/*
var internalCommands = [
  ['Internal', 'TEST', 'V', 'https://url.com', '/search?q={}', '#150000'],
];

internalCommands.forEach(function(command) {
 CONFIG.commands.push(command);
});
*/